<Page x:Class="WorldSkills.Pages.Auth.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="800"
      Title="Окно авторизации">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
            <StackPanel>
                <TextBlock Text="Введите логин" Style="{StaticResource LoginTxb}"/>
                <TextBox x:Name="LoginTxt" Style="{StaticResource LoginTxt}"/>
            </StackPanel>

            <StackPanel Margin="0 10 0 0">
                <TextBlock Text="Введите пароль" Style="{StaticResource LoginTxb}"/>
                <PasswordBox x:Name="PasswordPxt" Style="{StaticResource LoginPxt}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
                <Button x:Name="LoginBtn" Content="ВОЙТИ" Margin="0 0 10 0" Style="{StaticResource LoginButton}" Click="LoginBtn_Click"/>
                <Button x:Name="RegisterBtn" Content="РЕГИСТРАЦИЯ" Style="{StaticResource RegisterButton}" Click="RegisterBtn_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
